export {
  default,
  dirname,
  basename,
  extname,
  join,
  resolve,
  normalize,
  relative,
  isAbsolute,
  sep,
  delimiter,
  parse,
  format,
  win32,
  posix,
} from 'path';

export const toNamespacedPath = (path: string) => path;
